/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/Quiz',
    output: 'export',
    images: {
        unoptimized: true,
    },
    reactStrictMode: false,
};

export default nextConfig;
